// created by Chelsea Nnanyanzi
//defines the type Meme that follows the API's JSON structure
//id, name, and url were the only attributes kept, unnecessary attributes were omitted
export type Meme = {
    id: string;
    name:string,
    url:string,


};

